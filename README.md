# tianleh-test-utility


Install the latest node.

```
nvm install --lts
```

To publish the new change, increase the version in package.json and then run the following command.

```
npm publish --access public

```